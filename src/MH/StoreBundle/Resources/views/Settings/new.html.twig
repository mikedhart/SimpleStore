{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container">
        <div class="row">
            <div class="span12">

                <h1>Settings</h1>

                {{ form(form) }}

                <a class="btn" href="{{ path('admin_settings') }}">
                    Back
                </a>
            </div>
        </div>
    </div>

{% endblock %}
