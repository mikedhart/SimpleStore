{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Settings edit</h1>

    {{ form(edit_form) }}

        <a class="btn" href="{{ path('admin_settings') }}">
            Cancel
        </a>

{% endblock %}
